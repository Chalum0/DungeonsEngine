class Camera:
    def __init__(self, name_camera, pos_camera, type_camera):
        self._name = name_camera
        self._pos = pos_camera
        self._type = type_camera

    def _set_pos(self):
        self._pos = 
